# Environment Variables for Client-Side Application
# Copy this file to .env.local

# ================================================================
# IMPORTANT SECURITY NOTICE - HMRC COMPLIANCE
# ================================================================
# HMRC OAuth credentials (Client ID and Client Secret) must NEVER
# be stored client-side. They are stored securely in Firebase Secrets.
#
# To configure HMRC credentials:
# 1. Go to Google Cloud Console > Secret Manager
# 2. Add secrets: HMRC_CLIENT_ID and HMRC_CLIENT_SECRET
# 3. Deploy Firebase Functions to use these secrets
#
# See: functions/env.example for Firebase Functions configuration
# Reference: HMRC Development Practices - OAuth Security Requirements
# ================================================================

# ================================================================
# NETWORK CONFIGURATION - CRITICAL
# ================================================================
# HMRC Developer Hub Requirements:
#
# 1. SINGLE APPLICATION: Only ONE production application per vendor
#    - Name it after your company (e.g., "1Stop HR Platform")
#    - Do NOT create separate apps per customer
#
# 2. IP ADDRESS WARNING: HMRC uses DYNAMIC IP addresses
#    - DO NOT configure firewall rules based on IP addresses
#    - DO NOT whitelist specific IPs for HMRC endpoints
#    - IPs change without notice and will break your integration
#
# 3. DOMAIN-BASED ACCESS: Configure proxy/firewall by domain
#    REQUIRED DOMAINS:
#    - api.service.hmrc.gov.uk (production)
#    - test-api.service.hmrc.gov.uk (sandbox)
#
# 4. CORS: HMRC APIs do NOT support CORS
#    - All API calls MUST go through Firebase Functions proxy
#    - Direct browser-to-HMRC calls will fail
#
# 5. CERTIFICATES: Use system root CA keystore
#    - DO NOT import HMRC-specific certificates
#    - Node.js/browsers trust Mozilla's root CA bundle automatically
#
# See: HMRC_DEVELOPER_HUB_BEST_PRACTICES.md for full details
# ================================================================

# Firebase Functions Region (optional, defaults to us-central1)
# VITE_FIREBASE_FUNCTIONS_REGION=europe-west1

# Application Environment
VITE_APP_ENV=development

# ================================================================
# HMRC API CONFIGURATION
# ================================================================
# Base URL for Firebase Functions (used as HMRC proxy)
# Production should use your deployed Firebase Functions URL
# VITE_FIREBASE_FUNCTIONS_URL=https://us-central1-YOUR-PROJECT-ID.cloudfunctions.net

# HMRC OAuth Redirect URI (used for OAuth callback)
# VITE_HMRC_REDIRECT_URI=https://your-domain.com/hmrc/callback

# ================================================================
# TOKEN ENCRYPTION (REQUIRED FOR COMPLIANCE)
# ================================================================
# HMRC OAuth tokens must be encrypted at rest per HMRC requirements.
#
# IMPORTANT: For production, store the encryption key in Firebase Secrets:
#   firebase functions:secrets:set HMRC_ENCRYPTION_KEY
#
# The key must be at least 32 characters (256 bits for AES-256)
# Generate a secure key: openssl rand -base64 32
#
# For development/testing only (NEVER commit real keys):
# VITE_HMRC_ENCRYPTION_KEY=your-32-character-minimum-encryption-key

# ================================================================
# FIREBASE & STRIPE CONFIGURATION (Main 1Stop App)
# ================================================================
# Required for src/config/keys.ts - Main app Firebase and Stripe.
# Vite exposes only VITE_* prefixed vars to the client.
#
# Get Firebase config from Firebase Console > Project Settings > General
# Get Stripe key from Stripe Dashboard > Developers > API keys
# ================================================================

# Firebase (required for main app)
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_DATABASE_URL=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
VITE_FIREBASE_MEASUREMENT_ID=

# Stripe (optional - for payments)
VITE_STRIPE_PUBLISHABLE_KEY=

# ================================================================
# YOURSTOP / OLDYOURSTOP FIREBASE (optional)
# ================================================================
# When set, YourStop/OldYourStop use this project; otherwise they use main app Firebase (VITE_FIREBASE_* above).
# VITE_YOURSTOP_FIREBASE_API_KEY=
# VITE_YOURSTOP_FIREBASE_AUTH_DOMAIN=
# VITE_YOURSTOP_FIREBASE_DATABASE_URL=
# VITE_YOURSTOP_FIREBASE_PROJECT_ID=
# VITE_YOURSTOP_FIREBASE_STORAGE_BUCKET=
# VITE_YOURSTOP_FIREBASE_MESSAGING_SENDER_ID=
# VITE_YOURSTOP_FIREBASE_APP_ID=
# VITE_YOURSTOP_FIREBASE_MEASUREMENT_ID=

# ================================================================
# PWF-LOOR-APP (optional - tools/originals/pwf-loor-app only)
# ================================================================
# Required only if you use the pwf-loor-app tool. Get config from that Firebase project.
# VITE_PWF_LOOR_API_KEY=
# VITE_PWF_LOOR_AUTH_DOMAIN=
# VITE_PWF_LOOR_DATABASE_URL=
# VITE_PWF_LOOR_PROJECT_ID=
# VITE_PWF_LOOR_STORAGE_BUCKET=
# VITE_PWF_LOOR_MESSAGING_SENDER_ID=
# VITE_PWF_LOOR_APP_ID=
# VITE_PWF_LOOR_MEASUREMENT_ID=

# ================================================================
# CUSTOMER FIREBASE (optional - YourStop customer app only)
# ================================================================
# Only for separate customer-facing Firebase. In production, placeholder values are never used.
# VITE_CUSTOMER_FIREBASE_API_KEY=
# VITE_CUSTOMER_FIREBASE_PROJECT_ID=
# VITE_CUSTOMER_FIREBASE_APP_ID=
# (and other VITE_CUSTOMER_FIREBASE_* / NEXT_PUBLIC_CUSTOMER_FIREBASE_* as needed)
